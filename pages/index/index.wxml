<!--pages/index/index.wxml-->
<block wx:for="{{products}}"wx:key="key">
<van-card
  price="{{item.view}}"
  desc="{{item.color}}"
  title="{{item.title}}"
  thumb="{{item.image}}"
  bindtap ="click"
  data-id = "{{item._id}}"
/>
</block>>